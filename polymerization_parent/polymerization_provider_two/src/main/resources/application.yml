server:
  port: 6002
spring:
  application:
    name: provider-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        health-check-interval: 10s
        health-check-path: /actuator/health
